# گزارش بازبینی و نقشه‌راه تبدیل به افزونه کلاس‌جهانی

**افزونه:** Nafas Smart Chatbot — نسخهٔ ۳.۱.۰
**تاریخ گزارش:** ۱۴۰۵/۰۵/۰۵ (۲۰۲۶-۰۷-۲۷)
**دامنهٔ بررسی:** کل مخزن — هستهٔ PHP، لایهٔ AJAX/هوش مصنوعی، لایهٔ دیتابیس، پنل مدیریت، ویجت المنتور، فرانت‌اند (JS/CSS)، uninstall، بین‌المللی‌سازی و فایل‌های ابزار.
**روش:** بازبینی دستی کامل کد + ممیزی موجود (`PROJECT_AUDIT.md`).

> **جمع‌بندی یک‌خطی:** این افزونه از نظر فنی برای یک نیچ خاص (چت‌بات فارسی/RTL داروسازی) بسیار خوب ساخته شده و پایهٔ امنیتی‌اش بالاتر از میانگین افزونه‌های وردپرس است. اما برای «فروش عمومی در مارکت» دو کار بزرگ لازم دارد: (۱) **عمومی‌سازی و حذف کامل هویت «نفس فارمد»** تا هر کسب‌وکاری بتواند استفاده کند، و (۲) رفع چند باگ امنیتی/داده‌ای و بستن شکاف‌های استاندارد مارکت (i18n دوزبانه، دسترس‌پذیری، تست، مستندات).

---

## بخش ۰ — اولویت اجرا (خلاصهٔ مدیریتی)

| اولویت | موضوع | چرا | تخمین کار |
|---|---|---|---|
| 🔴 فاز ۱ | عمومی‌سازی و حذف برند «نفس» (بخش ۱) | بدون این، افزونه قابل‌فروش عمومی نیست | متوسط–بالا |
| 🔴 فاز ۱ | رفع باگ‌های امنیتی/داده‌ای (بخش ۲) | تزریق فرمول CSV، دورزدن Rate limit، از دست رفتن داده | متوسط |
| 🟠 فاز ۲ | استانداردهای مارکت: i18n دوزبانه، LTR، اعتبارسنجی تلفن بین‌المللی (بخش ۳) | لازمهٔ فروش جهانی | متوسط |
| 🟠 فاز ۲ | دسترس‌پذیری (WCAG) و کیفیت UX (بخش ۴) | رد شدن در بازبینی مارکت‌های معتبر | متوسط |
| 🟡 فاز ۳ | تست، CI، مستندات، معماری (بخش ۵) | پایداری بلندمدت و اعتماد خریدار | بالا |
| 🟢 فاز ۴ | قابلیت‌های تمایزبخش و مدل درآمدی (بخش ۶) | تبدیل از «خوب» به «کلاس‌جهانی» | بالا |

---

## بخش ۱ — عمومی‌سازی: حذف کامل هویت «نفس فارمد» (مهم‌ترین درخواست شما)

افزونه در حال حاضر در ده‌ها نقطه به شرکت «نفس زیست فارمد» و حوزهٔ داروسازی گره خورده است. برای اینکه «هرکسی بتواند استفاده کند» باید این‌ها را به مقادیر خنثی/قابل‌تنظیم تبدیل کرد. جدول زیر همهٔ نقاط گره‌خوردگی را با راه‌حل مشخص می‌کند.

### ۱-۱. برندینگ متنی سخت‌کدشده

| محل | مقدار فعلی | راه‌حل |
|---|---|---|
| `nafas-chatbot.php` (هدر افزونه) | نام «Nafas Smart Chatbot»، Plugin URI به `patient.nafaspharmed.com`، Author «Nafas Pharmed» | نام عمومی مثل «Smart Support Chatbot» یا برند محصولی خنثی؛ URI و Author به سایت/برند خودتان |
| `class-nafas-chatbot-settings.php` → `defaults()` | `company_name = 'شرکت نفس زیست فارمد'`، `company_id = 'nafas'` | پیش‌فرض خنثی: `company_name = ''` یا «شرکت شما»، `company_id = 'company'` |
| همان فایل | `welcome_text = 'به پورتال پشتیبانی نفس فارمد...'` | «به پشتیبانی آنلاین ما خوش آمدید» |
| همان فایل | لیست ۵ محصول واقعی نفس (کپسولایزر، کلدانیز، …) | پیش‌فرض: آرایهٔ خالی یا یک محصول نمونهٔ عمومی «محصول نمونه» |
| همان فایل | `ai_system_prompt` که صریحاً «شرکت داروسازی نفس زیست فارمد» را نام می‌برد | پرامپت عمومی با placeholder: «شما دستیار هوشمند {company_name} هستید…» |
| همان فایل | `ai_fallback_msg` که «نفس فارمد» را نام می‌برد | متن خنثی بدون نام شرکت |
| `class-nafas-chatbot.php` → `add_privacy_policy_content()` | متن «دستیار هوشمند نفس» | استفاده از `get_bloginfo('name')` یا `company_name` تنظیمات |
| `class-nafas-chatbot-ajax.php` → `build_notification_text()` | «📥 دریافت درخواست جدید از پورتال آموزش بیمار» | عنوان قابل‌تنظیم یا `get_bloginfo('name')` |
| `assets/js/nafas-chatbot.js` (فوتر چت) | `'Developed by Saeed &amp; Claude'` **سخت‌کدشده و قابل‌مشاهده برای بازدیدکنندهٔ نهایی** | حذف کامل از UI مشتری، یا تبدیل به گزینهٔ «Powered by» قابل‌خاموش‌کردن در نسخهٔ Pro |
| `includes/views/about-page.php` | نام‌های «Claude» و «سعید» به‌صورت اعتبار توسعه‌دهنده | جایگزینی با برند خودتان یا حذف؛ اعتبار توسعه در فروش عمومی مشکلی ندارد ولی نباید در UI مشتری دیده شود |

**راهکار معماری پیشنهادی:** یک تابع مرکزی مثل `Nafas_Chatbot_Settings::brand( $key )` بسازید که همهٔ متن‌های برندی از آن بخوانند، و در فعال‌سازی افزونه مقادیر پیش‌فرض را از `get_bloginfo('name')` سایت پر کنید تا افزونه از همان ابتدا «خنثی» باشد.

### ۱-۲. تغییر نام فضای‌نام کد (namespace / prefix)

همهٔ کلاس‌ها `Nafas_Chatbot_*`، ثابت‌ها `NAFAS_CHATBOT_*`، توابع `nafas_chatbot_*`، کلید options `nafas_chatbot_settings`، جداول `nafas_chatbot_*` و text-domain برابر `nafas-chatbot` است.

- **برای انتشار عمومی، کل پیشوند را به یک نام محصول خنثی تغییر دهید** (مثلاً `ssc_` / `SmartChatbot_` / text-domain = `smart-chatbot`). این یک refactor مکانیکی اما گسترده است.
- **نکتهٔ مهاجرت:** چون نام جداول و کلید options تغییر می‌کند، باید یک روال مهاجرت یک‌بارهٔ داده‌ها بنویسید (کپی جداول و options قدیمی به نام جدید) تا نصب‌های موجود داده‌شان را از دست ندهند. اگر افزونه تاکنون منتشر عمومی نشده، این کار ساده‌تر است و فقط نام‌گذاری تازه لازم است.
- text-domain باید **دقیقاً برابر اسلاگ افزونه** باشد (الزام ووردپرس‌داتorg).

### ۱-۳. ماژولار کردن بخش «عوارض دارویی» (ADR / فارماکوویژیلانس)

قوی‌ترین گره به داروسازی، فرم استاندارد ثبت عوارض دارویی (شدت، پیامد، شماره Batch، داروهای همزمان، نوع گزارش‌دهنده) و ستون‌های اختصاصی دیتابیس آن است. یک فروشگاه یا شرکت خدماتی این را لازم ندارد.

**راه‌حل:** بخش ADR را به یک **ماژول قابل‌فعال/غیرفعال** تبدیل کنید:
- در تنظیمات یک سوییچ «حالت کسب‌وکار»: `عمومی` / `داروسازی` / `سفارشی`.
- در حالت «عمومی»، منوی «ثبت عوارض» و همهٔ فیلدهای ADR پنهان شوند و فقط دو گزینهٔ عمومی «سوال از ما» و «درخواست تماس/مشاوره» بمانند.
- سیستم فرم‌ساز را عمومی کنید: به‌جای فیلدهای ثابت ADR، یک **فرم‌ساز فیلد پویا** (Field Builder) بدهید تا هر کسب‌وکاری فیلدهای دلخواه خودش (مثلاً «شماره سفارش»، «نوع خدمت») را بسازد. این هم مشکل داروسازی را حل می‌کند و هم یک قابلیت فروش قوی می‌شود.
- ستون‌های ADR در دیتابیس بمانند (بی‌ضرر هستند) اما فقط در حالت داروسازی نمایش داده شوند.

### ۱-۴. وابستگی‌های محلی/منطقه‌ای

| مورد | مشکل عمومیت | راه‌حل |
|---|---|---|
| اعتبارسنجی تلفن `^(\+98|0)?9\d{9}$` در `handle_submit()` | فقط موبایل ایران را می‌پذیرد | اعتبارسنجی بین‌المللی (E.164) یا قابل‌تنظیم بر اساس کشور؛ حداقل یک فیلتر `apply_filters` برای الگو |
| پیام‌رسان «بله» (`tapi.bale.ai`) | مخصوص ایران | نگه‌داشتن به‌عنوان یک گزینه، افزودن تلگرام (که هست) + WhatsApp/Slack/Discord webhook برای بازار جهانی |
| فونت Vazirmatn از CDN jsdelivr | فقط فارسی + وابستگی به CDN خارجی (خلاف «آفلاین‌محور») | بارگذاری محلی فونت (bundle) و فقط در حالت RTL؛ در LTR از فونت سیستمی استفاده شود |
| RTL و متن‌های فارسی سخت‌کد در JS | جهان‌شمول نیست | همهٔ رشته‌ها از طریق `wp_localize_script`/`i18n` و پشتیبانی دوجهته (بخش ۳) |

---

## بخش ۲ — باگ‌ها و مشکلات امنیتی/داده‌ای (با راه‌حل)

این موارد از ممیزی فنی استخراج و بازتأیید شده‌اند. اولویت رفع بالا دارند چون یا امنیت back-office را تهدید می‌کنند یا باعث از دست رفتن خاموش داده می‌شوند.

### 🔴 ۲-۱. تزریق فرمول در خروجی CSV
- **کجا:** `class-nafas-chatbot-admin.php` → `export_csv()`.
- **مشکل:** مقادیر واردشدهٔ کاربر (نام، شرح، داروهای همزمان و…) بدون پاکسازی در CSV نوشته می‌شوند. مهاجم می‌تواند در فرم مقداری مثل `=cmd|'/C calc'!A0` بفرستد که هنگام باز کردن فایل در Excel توسط کارشناس اجرا می‌شود.
- **راه‌حل:** هر سلولی که با `= + - @ Tab CR` شروع می‌شود را با یک `'` ابتدایی خنثی کنید:
  ```php
  private static function csv_safe( $v ) {
      $v = (string) $v;
      return preg_match( '/^[=+\-@\t\r]/', $v ) ? "'" . $v : $v;
  }
  ```
  و روی همهٔ سلول‌ها قبل از `fputcsv()` اعمال کنید.

### 🟠 ۲-۲. دورزدن Rate limit در حالت session
- **کجا:** `check_rate_limit()`.
- **مشکل:** در حالت `session`، کلید محدودیت از فیلد `cid` که **کاملاً سمت کلاینت** ساخته می‌شود گرفته می‌شود. مهاجم با فرستادن `cid` تصادفی در هر درخواست، سهمیهٔ نامحدود می‌گیرد و می‌تواند بودجهٔ توکن API پولی را تخلیه کند. در این حالت هیچ سقف IP پشتیبانی وجود ندارد.
- **راه‌حل:** در حالت `session` همیشه یک سقف IP بالاتر (مثلاً `۱۰×` سهمیهٔ session) به‌عنوان backstop نگه دارید؛ یا `cid` را سمت سرور به‌صورت کوکی امضاشده (`hash_hmac(..., AUTH_KEY)`) صادر و اعتبارسنجی کنید.

### 🟠 ۲-۳. شمارندهٔ Rate limit اتمیک نیست (Race condition)
- **کجا:** `check_rate_limit()` — الگوی `get_transient()` سپس `set_transient(n+1)`.
- **مشکل:** خواندن-تغییر-نوشتن روی transient در همزمانی، دچار شرایط مسابقه می‌شود و محدودیت کم‌شماری می‌کند.
- **راه‌حل:** از همان الگوی اتمیک آماری موجود استفاده کنید: `INSERT ... ON DUPLICATE KEY UPDATE cnt = cnt + 1` روی جدول `stats` (metric = `rl:{bucket}:{hash}`) یا `wp_cache_incr()`.

### 🟠 ۲-۴. سمی‌سازی آمار/بازخورد از endpointهای بدون‌احراز
- **کجا:** `handle_feedback()`، `handle_csat()`، `handle_suggest()`، `record_chat()`.
- **مشکل:** هر بازدیدکننده می‌تواند: امتیاز 👎 روی هر `log_id` (اعداد ترتیبی و قابل‌شمارش) بگذارد؛ رأی CSAT نامحدود بدهد؛ با هر keystroke یک fetch+امتیازدهی کامل DB اجرا کند (اهرم DoS)؛ متریک‌های `product:{هر رشته‌ای}` را بی‌نهایت بسازد و داشبورد را آلوده کند.
- **راه‌حل:**
  - بازخورد: با پاسخ چت یک توکن HMAC per-log بفرستید و در feedback بخواهید؛ فقط ردیف‌هایی با `rating = 0` را به‌روزرسانی کنید (رأی مجدد نادیده).
  - CSAT: محدودسازی per-cid/IP (روزی یک رأی کافی است).
  - suggest: افزودن به rate limiter با سقف سخاوتمند.
  - stats: اعتبارسنجی `product` در برابر `products_map()` + `company_id`؛ هر مقدار ناشناخته به `general` نگاشت شود.

### 🟠 ۲-۵. کلید API جمینای در Query String
- **کجا:** `gemini_reply()` — `?key=API_KEY` در URL.
- **مشکل:** کلید در لاگ سرور/پروکسی/دسترسی ثبت می‌شود.
- **راه‌حل:** کلید را در هدر `x-goog-api-key` بفرستید (Gemini پشتیبانی می‌کند) نه در URL.

### 🔴 ۲-۶. از دست رفتن خاموش بانک Q&A به‌خاطر `max_input_vars`
- **کجا:** ذخیرهٔ فرم بانک پاسخ‌ها (`save_qa_bank`) که ردیف‌ها را به‌صورت آرایه‌های `qa_question[]` می‌فرستد.
- **مشکل:** با انبوه شدن بانک، تعداد فیلدهای POST از `max_input_vars` (پیش‌فرض ۱۰۰۰) عبور می‌کند و PHP بی‌صدا ورودی را می‌بُرد → ردیف‌ها بدون هشدار حذف می‌شوند.
- **راه‌حل:** ویرایش بانک را به AJAX per-row (ذخیره/حذف تک‌ردیفی) تبدیل کنید؛ یا حداقل تعداد فیلدها را قبل از ذخیره چک کرده و اگر نزدیک سقف بود هشدار دهید.

### 🟠 ۲-۷. صفر شدن `usage_count` در هر ذخیرهٔ بانک
- **کجا:** `qa_replace_all()` که کل جدول را DELETE+INSERT می‌کند.
- **مشکل:** شمارندهٔ استفادهٔ هر سوال (که برای «چیپس‌های مرتبط» و آنالیتیکس مهم است) با هر ذخیره صفر می‌شود.
- **راه‌حل:** به upsert بر اساس شناسه/کلید یکتا مهاجرت کنید تا `usage_count` حفظ شود؛ یا در حالت جایگزینی، مقادیر قبلی را بر اساس تطبیق سوال بازیابی کنید.

### 🟡 ۲-۸. سایر موارد تأییدشده (خلاصه)
- **حذف نشدنی آخرین محصول/Quick reply:** فرم فقط ردیف‌های ارسالی را می‌خواند؛ حذف همه = بازگشت به پیش‌فرض. راه‌حل: فیلد پرچم «خالی عمدی».
- **شناسهٔ محصول فارسی دور ریخته می‌شود:** `sanitize_key()` کاراکتر غیرلاتین را حذف می‌کند. راه‌حل: تولید خودکار slug یا پذیرش شناسهٔ یونیکد.
- **ایمپورت CSV با پاسخ چندخطیِ نقل‌قول‌دار می‌شکند:** چون خط‌به‌خط `str_getcsv` می‌شود. راه‌حل: استفاده از `fgetcsv` روی استریم کامل.
- **`DATETIME DEFAULT '0000-00-00 00:00:00'`** روی MySQL مدرن (strict mode) خطا می‌دهد. راه‌حل: `NULL` یا `CURRENT_TIMESTAMP`.
- **اکشن‌های تغییر وضعیت روی GET:** حذف/تغییر وضعیت با لینک nonce‌دار GET؛ بهتر است POST باشد (nonce موجود است اما GET برای عملیات تغییردهنده ضعیف‌تر است).
- **افکت تایپ (typewriter)** با کاربر می‌جنگد و Markdown را لحظه‌ای خام نشان می‌دهد. راه‌حل: احترام به `prefers-reduced-motion` و رندر نهایی یک‌جا.

---

## بخش ۳ — استانداردهای لازم برای فروش جهانی

### ۳-۱. بین‌المللی‌سازی واقعی (i18n)
- بخش بزرگی از رشته‌ها **در JS و در پیش‌فرض‌های PHP فارسیِ سخت‌کد** هستند و از سیستم ترجمه عبور نمی‌کنند (متن دکمه‌ها، پیام‌های ربات، «Developed by…»، جملات فرم).
- **راه‌حل:** همهٔ رشته‌های UI را به `__()/esc_html__()` منتقل و از طریق `wp_localize_script` به JS بدهید (بخشی انجام شده، اما ناقص). فایل `.pot` را کامل بازتولید کنید. یک زبان **پیش‌فرض انگلیسی** اضافه کنید و فارسی را به‌صورت ترجمهٔ همراه (`languages/*-fa_IR.po`) عرضه کنید.

### ۳-۲. پشتیبانی دوجهته (RTL + LTR)
- کل CSS و `dir="rtl"` ثابت است. برای بازار جهانی باید layout بر اساس زبان سایت یا تنظیم افزونه، LTR هم بشود. از خصوصیات منطقی CSS (`margin-inline`, `inset-inline`) استفاده کنید.

### ۳-۳. اعتبارسنجی تلفن و داده بین‌المللی
- الگوی موبایل ایران را با یک اعتبارسنجی عمومی‌تر یا انتخاب کشور جایگزین کنید (بخش ۱-۴).

### ۳-۴. سازگاری اعلام‌شده و نسخه‌ها
- `readme.txt`: `Tested up to: 6.7` — با هر نسخهٔ جدید وردپرس به‌روزرسانی شود؛ `Requires PHP: 7.2` قدیمی است (7.4/8.0 پیشنهاد می‌شود).
- مدل‌های پیش‌فرض هوش مصنوعی سخت‌کد شده‌اند (`gemini-2.0-flash`, `gpt-4o-mini`, `claude-opus-4-8`). این‌ها مرتب کهنه می‌شوند؛ فهرست مدل‌ها را از یک فیلتر/تنظیم قابل‌به‌روزرسانی بخوانید و پیش‌فرض‌ها را به جدیدترین مدل‌های هر ارائه‌دهنده نگه دارید (برای Claude، خانوادهٔ Claude Opus/Sonnet نسل جدید).

---

## بخش ۴ — دسترس‌پذیری (WCAG) و کیفیت UX

مارکت‌های معتبر (و بسیاری از مشتریان سازمانی) بدون دسترس‌پذیری قبول نمی‌کنند.

- **پنجرهٔ چت یک dialog نیست:** بدون `role="dialog"`, `aria-modal`, مدیریت فوکوس و focus-trap. با باز شدن، فوکوس باید به داخل پنجره برود و با Esc برگردد.
- **وضعیت دکمهٔ toggle** با `aria-expanded` منتقل نمی‌شود؛ live-region روی کل ریشه (`aria-live="polite"`) بدطراحی است و کل رندر دوباره را اعلام می‌کند.
- **فیلدهای فرم** برچسب برنامه‌ای مطمئن ندارند (اتکای صرف به آیکن/placeholder).
- **بدون `prefers-reduced-motion`** برای انیمیشن‌ها.
- **کنتراست رنگ** رنگ برند قرمز پیش‌فرض روی برخی حالت‌ها را بررسی کنید (AA).
- **`wp_kses_post` روی کانتینر شناور، `aria-live` را حذف می‌کند** (چون در allowlist نیست) — یعنی حتی همان live-region موجود در خروجی نهایی حذف می‌شود.

**راه‌حل:** یک پاس دسترس‌پذیری کامل با الگوی «Dialog Modal» ARIA، مدیریت فوکوس، و تست با screen reader.

---

## بخش ۵ — معماری، کیفیت کد و پایداری

- **God Class:** `Nafas_Chatbot_Ajax` حدود ۱۳۷۰ خط است و مسئولیت‌های چت، rate limit، تطبیق فارسی، RAG، و ادغام همهٔ ارائه‌دهنده‌های AI را دارد. راه‌حل: جدا کردن به کلاس‌های `AI_Provider` (با اینترفیس مشترک)، `Matcher`, `RateLimiter`.
- **فرانت‌اند یک IIFE ~۱۳۰۰ خطی با رندر کامل DOM در هر تعامل** است. راه‌حل بلندمدت: بازنویسی به‌صورت Web Component با رندر افزایشی و یک pipeline ساخت (esbuild/vite).
- **صفر تست خودکار، بدون CI.** حداقل: تست‌های واحد PHPUnit برای لایهٔ تطبیق/دیتابیس، و یک workflow گیت‌هاب برای PHPCS + تست. این برای اعتماد خریدار و بازبینی مارکت حیاتی است (`.phpcs.xml` هست ولی CI نیست).
- **`nafas-chatbot.zip` داخل گیت commit شده** — artifact ساخت نباید در مخزن باشد؛ به `.gitignore` و خروجی build منتقل شود.
- **منطق query-builder تکراری** بین `get_submissions` و `get_filtered_for_export` — یکپارچه شود.
- **طراحی options یک‌تکه (۶۰+ کلید در یک option سریالایزشده)** — قابل قبول، اما با رشد `product_knowledge` بزرگ می‌شود؛ autoload=no انجام شده (خوب است).
- **cron از سازندهٔ کلاس زمان‌بندی می‌شود** و `flush_rewrite_rules()` بی‌مورد در فعال/غیرفعال؛ تمیزکاری شود.
- **مستندات:** README دوتایی (`README.md`, `README-FA.md`)؛ برای مارکت به مستندات نصب، تنظیم هر ارائه‌دهندهٔ AI، شورت‌کد، هوک‌ها و FAQ نیاز دارید.

---

## بخش ۶ — قابلیت‌های تمایزبخش (تبدیل به «کلاس‌جهانی» و قابل‌فروش)

این‌ها افزونه را از «یک چت‌بات خوب» به محصول رقابتی مارکت تبدیل می‌کنند:

1. **فرم‌ساز پویا (Field Builder):** هر کسب‌وکار فرم دلخواه خودش را بسازد (این هم مشکل عمومیت را حل می‌کند و هم قابلیت فروش قوی است).
2. **پاسخ استریم (SSE):** به‌جای صبر برای پاسخ کامل، توکن‌به‌توکن نمایش دهید (تجربهٔ ChatGPT).
3. **بازیابی برداری واقعی (Vector RAG):** موتور فعلی لغوی/مترادف است؛ افزودن embeddings (با fallback آفلاین موجود) دقت را بالا می‌برد.
4. **گفتگوی سمت‌سرور و پایداری مکالمه بین صفحات:** الان با هر ناوبری، گفتگو پاک می‌شود (F1 ممیزی). ذخیرهٔ نشست سمت سرور یا `sessionStorage`.
5. **واگذاری به اپراتور زنده و اینباکس چندکاناله:** رقابت با ویجت‌های تجاری.
6. **ماژول انطباق (برای نسخهٔ داروسازی):** خروجی استاندارد فارماکوویژیلانس به‌صورت افزونهٔ اختیاری/پولی.
7. **مدل درآمدی:** هستهٔ رایگان + Pro (فرم‌ساز، چندکاناله، برندینگ سفید، مدل‌های پیشرفته). حذف «Developed by…» از UI مشتری در Pro یک اهرم فروش کلاسیک است.
8. **لایهٔ اطمینان برای فراخوانی AI:** retry با backoff، circuit breaker، و صف async برای تماس‌های کند (الان ۶۰ ثانیه به‌صورت همزمان worker PHP را نگه می‌دارد → اهرم DoS ظرفیتی).
9. **مانیتورینگ و آنالیتیکس:** داشبورد بینش (سوالات بی‌پاسخ، CSAT، بازخورد) خوب شروع شده؛ افزودن نمودار روند، نرخ حل، و صادرات گزارش.
10. **بلوک گوتنبرگ + شورت‌کد + ویجت المنتور:** الان المنتور و شورت‌کد هست؛ افزودن بلوک بومی گوتنبرگ برای پوشش کامل.

---

## پیوست الف — چک‌لیست اجرایی پیشنهادی (به‌ترتیب)

1. عمومی‌سازی برند (بخش ۱-۱، ۱-۴) + سوییچ «حالت کسب‌وکار» و ماژولار کردن ADR (۱-۳).
2. رفع باگ‌های امنیتی: CSV (۲-۱)، rate limit (۲-۲، ۲-۳)، سمی‌سازی آمار (۲-۴)، کلید جمینای (۲-۵).
3. رفع باگ‌های داده‌ای: `max_input_vars` (۲-۶)، `usage_count` (۲-۷)، DATETIME (۲-۸).
4. i18n دوزبانه + LTR + تلفن بین‌المللی + پیش‌فرض انگلیسی (بخش ۳).
5. پاس دسترس‌پذیری (بخش ۴).
6. تست/CI + حذف zip از گیت + مستندات مارکت (بخش ۵).
7. تغییر نام namespace با روال مهاجرت داده (۱-۲) — پیش از اولین انتشار عمومی.
8. قابلیت‌های تمایزبخش و مدل درآمدی (بخش ۶).

## پیوست ب — نقاط قوت فعلی (که باید حفظ شوند)
- پایهٔ امنیتی خوب: nonce همه‌جا، prepared statements، رمزنگاری کلیدها با AUTH_KEY، honeypot + تلهٔ زمان.
- موتور تطبیق فارسی با مترادف و ایندکس FULLTEXT مقیاس‌پذیر.
- معماری چندارائه‌دهندهٔ AI (Gemini/OpenAI/Claude/Custom/Webhook) با تست اتصال.
- RAG سبک آفلاین (مناسب شرایط تحریم) — یک تمایز واقعی.
- آمار اتمیک (race-safe) و پاک‌سازی خودکار داده برای حریم خصوصی.
- UX مدرن: کارت محصول، افکت تایپ، صوت (STT/TTS)، CSAT، چیپس پیگیری.
