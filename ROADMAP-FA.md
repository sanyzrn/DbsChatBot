# رودمپ ارتقا به افزونهٔ کلاس‌حرفه‌ای و قابل‌فروش

**پروژه:** Smart Support Chatbot (نسخهٔ فعلی ۳.۲.۰)
**تاریخ:** ۱۴۰۵/۰۵/۰۵ — ۲۰۲۶-۰۷-۲۷
**هدف:** تبدیل از «افزونهٔ اختصاصی خوب» به «محصول قابل‌انتشار و قابل‌فروش در مارکت»

---

## ۰. خلاصهٔ مدیریتی — کجا ایستاده‌ایم؟

بررسی مجدد و مستقیم کد (نه اتکا به ممیزی قبلی) این تصویر را می‌دهد:

| بُعد | وضعیت | نمره (۱۰) |
|---|---|---|
| قابلیت‌ها (Features) | غنی و رقابتی — چند موتور AI، RAG آفلاین، بانک پاسخ، CRM درخواست‌ها، صوت، CSAT | **8** |
| امنیت | پایهٔ خوب (nonce، prepared، رمزنگاری کلید) اما ۵ مورد باز | **6** |
| معماری و کیفیت کد | بدهی جدی: کلاس ۱۳۸۶ خطی، فرانت IIFE ۱۳۰۲ خطی، بدون REST | **4** |
| تست و CI | **صفر** — بدون تست، بدون CI، بدون build | **0** |
| دسترس‌پذیری (a11y) | تقریباً هیچ — بدون dialog/focus-trap، فقط ۱ `aria-label` | **2** |
| بین‌المللی‌سازی | فارسی‌محور — ۴۳ رشتهٔ فارسی سخت‌کد در JS فرانت | **3** |
| آمادگی مارکت (docs/demo/license) | وجود ندارد | **1** |

**واقع‌بینانه:** الان یک «افزونهٔ سفارشی بسیار خوب» است، نه یک «محصول». فاصله تا محصول عمدتاً در چهار ستون **صفر یا نزدیک صفر** است: تست/CI، دسترس‌پذیری، انگلیسی‌سازی، و زیرساخت انتشار. خبر خوب: هستهٔ فنی ارزشمند است و ارزش سرمایه‌گذاری دارد.

---

## ۱. تصمیم استراتژیک اول: کدام بازار؟ (مهم‌تر از هر تصمیم فنی)

قبل از یک خط کد، باید این را انتخاب کنید چون همه‌چیز را تغییر می‌دهد:

| مسیر | بازار | مزیت | هزینه | زمان تا درآمد |
|---|---|---|---|---|
| **الف) بازار ایران** | ژاکت، راست‌چین، فروش مستقیم | رقابت ضعیف، RTL شما عالی است، فارسی مزیت است نه بدهی | کم — با ۲–۳ هفته کار قابل عرضه | **کوتاه (۱ ماه)** |
| **ب) WordPress.org رایگان + Pro** | جهانی | قیف بازاریابی رایگان و عظیم، اعتبار | زیاد — انگلیسی‌سازی کامل، بازبینی سخت‌گیرانه | متوسط (۳–۴ ماه) |
| **ج) CodeCanyon** | جهانی پولی | فروش یک‌جا، ترافیک آماده | متوسط — دمو + مستندات + پشتیبانی الزامی | متوسط (۲–۳ ماه) |

### 💡 توصیهٔ من: مسیر دوگانه (الف ← ب)

**فاز اول بازار ایران را بگیرید تا کش‌فلو ایجاد شود، و با آن درآمد، مسیر جهانی را بسازید.**

دلیل: نقطهٔ قوت بی‌رقیب شما — **RAG کاملاً آفلاین بدون نیاز به سرویس embedding** — دقیقاً همان چیزی است که بازار ایران (تحریم، اینترنت ناپایدار، بودجهٔ ارزی) به آن نیاز دارد و رقبای جهانی آن را ندارند. در بازار جهانی این یک ویژگی «خوب» است؛ در ایران یک ویژگی «حیاتی».

**نکتهٔ مهم:** مسیر (ب) و (ج) متناقض نیستند — CodeCanyon اجازهٔ فروش نسخه‌ای که در WP.org رایگان است را نمی‌دهد، اما مدل «رایگان در WP.org + Pro در سایت خودتان (Freemius/Lemon Squeezy)» استاندارد صنعت است و بهترین بازده بلندمدت را دارد.

---

## ۲. پوزیشنینگ و تمایز — چرا کسی این را بخرد؟

بازار چت‌بات وردپرس **شلوغ** است (AI Engine، Tidio، Chatbase، WPBot و…). صادقانه: با «یک چت‌بات AI دیگر» شکست می‌خورید. باید روی تمایز واقعی بایستید.

**سه تمایز واقعی شما (بر اساس کد موجود، نه آرزو):**

1. **🔌 کار بدون وابستگی به سرویس برداری (Offline Hybrid RAG)**
   موتور تطبیق فارسی + FULLTEXT + مترادف، بدون embeddings و بدون vector DB. رقبا همه به OpenAI embeddings یا Pinecone وابسته‌اند. → پیام فروش: *«حتی وقتی API قطع است، ربات شما جواب می‌دهد.»*

2. **🔓 آزادی انتخاب موتور (Provider-agnostic)**
   Gemini/OpenAI/Claude/**هر endpoint سازگار با OpenAI** (OpenRouter، Groq، DeepSeek، Ollama، مدل محلی). → پیام فروش: *«مدل خودت را بیاور — حتی روی سرور خودت.»* این برای بازار حریم‌خصوصی‌محور اروپا هم جذاب است.

3. **🌍 کیفیت RTL/فارسی در سطحی که تقریباً هیچ رقیبی ندارد**

**پیشنهاد پوزیشنینگ:** «چت‌بات هوشمند پشتیبانی که مالک داده و مدلش خودتان هستید» — نه «ارزان‌ترین»، نه «هوشمندترین»، بلکه **«مستقل‌ترین»**.

---

## ۳. شکاف‌های مسدودکننده (تأییدشده با بررسی کد)

این‌ها قبل از هر انتشاری باید بسته شوند.

### 🔴 مسدودکنندهٔ انتشار

| # | مشکل | محل | راه‌حل |
|---|---|---|---|
| B-1 | **بدون تست و CI** — هیچ تستی وجود ندارد | کل پروژه | PHPUnit + WP test suite، GitHub Actions |
| B-2 | **بارگذاری فونت از CDN خارجی** (jsdelivr + Google Fonts) | `frontend.php:56,228,232` | **میزبانی محلی فونت.** Google Fonts در اروپا ریسک GDPR دارد (آرای دادگاه آلمان) و در ایران هم CDN ناپایدار است |
| B-3 | **دسترس‌پذیری تقریباً صفر** — بدون `role="dialog"`، بدون focus-trap، ۱ عدد `aria-label` در کل ویجت، بدون `prefers-reduced-motion` | `smart-support-chatbot.js` | پیاده‌سازی الگوی ARIA Dialog کامل |
| B-4 | **۴۳ رشتهٔ فارسی سخت‌کد در JS فرانت** | `smart-support-chatbot.js` | انتقال کامل به `wp_localize_script` + انگلیسی به‌عنوان زبان پایه |
| B-5 | **readme.txt ناقص** — فاقد FAQ، Screenshots، Upgrade Notice | `readme.txt` | تکمیل + افشای صریح تماس با سرویس‌های خارجی (الزام WP.org) |
| B-6 | **PHP 7.2** (پایان پشتیبانی از ۲۰۲۰) | هدر افزونه | ارتقا به PHP 7.4 یا 8.0 |

### 🟠 امنیت (باز از ممیزی قبلی)

| # | مشکل | راه‌حل |
|---|---|---|
| S-1 | **کلید API جمینای در Query String** (`?key=…`) — در لاگ سرور/پروکسی ثبت می‌شود | ارسال در هدر `x-goog-api-key` |
| S-2 | **سمی‌سازی بازخورد** — هر بازدیدکننده می‌تواند روی هر `log_id` (اعداد ترتیبی) رأی 👎 بدهد، بی‌نهایت بار | توکن HMAC per-log + شرط `rating = 0` |
| S-3 | **CSAT نامحدود** — میانگین رضایت کاملاً جعل‌پذیر | محدودسازی per-session/IP |
| S-4 | **`handle_suggest` بدون Rate limit** — هر keystroke یک fetch+scoring کامل DB (اهرم DoS) | افزودن به rate limiter |
| S-5 | **متریک محصول اعتبارسنجی نمی‌شود** — سیل متریک زبالهٔ بی‌نهایت در جدول آمار | اعتبارسنجی مقابل `products_map()` |

### 🟡 از دست رفتن خاموش داده

| # | مشکل | راه‌حل |
|---|---|---|
| D-1 | **سقف ~۲۵۰ ردیفی بانک Q&A** — ۴ فیلد آرایه‌ای در هر ردیف؛ با عبور از `max_input_vars` (پیش‌فرض ۱۰۰۰) PHP بی‌صدا ورودی را می‌بُرد | ویرایش AJAX تک‌ردیفی |
| D-2 | **`usage_count` در هر ذخیره صفر می‌شود** (DELETE+INSERT) | upsert بر اساس شناسه |
| D-3 | **شناسهٔ محصول فارسی دور ریخته می‌شود** (`sanitize_key`) | تولید خودکار slug |
| D-4 | **ایمپورت CSV با پاسخ چندخطی می‌شکند** (`str_getcsv` خط‌به‌خط) | `fgetcsv` روی استریم |
| D-5 | **`DATETIME DEFAULT '0000-00-00'`** (۴ مورد) — خطا در MySQL strict mode | `NULL` یا `CURRENT_TIMESTAMP` |

### 🟡 مقیاس‌پذیری

- **P-1: فراخوانی همزمان ۶۰ ثانیه‌ای AI** یک worker پی‌اچ‌پی را قفل می‌کند → با ۲۰ کاربر همزمان، سایت می‌خوابد. **این جدی‌ترین مشکل مقیاس است.**
- **P-2: بدون REST API** — همه‌چیز روی `admin-ajax.php` (کندتر، بدون کش، تست‌ناپذیرتر).
- **P-3: بدون پشتیبانی مالتی‌سایت** و بدون مدل نقش (فقط `manage_options`).

---

## ۴. رودمپ اجرایی

> تخمین‌ها بر اساس یک توسعه‌دهندهٔ تمام‌وقت است و **تخمین‌اند، نه تعهد**. اندازه: S = چند روز، M = ۱–۲ هفته، L = ۳–۴ هفته، XL = بیش از یک ماه.

### 🚀 نسخهٔ ۴.۰ — «آمادهٔ عرضه» (هدف: اولین فروش)
**تمرکز: بستن مسدودکننده‌ها. بدون قابلیت جدید.**

| # | کار | اندازه |
|---|---|---|
| 1 | رفع ۵ مورد امنیتی (S-1…S-5) | M |
| 2 | رفع ۵ مورد از دست رفتن داده (D-1…D-5) | M |
| 3 | میزبانی محلی فونت‌ها (حذف کامل CDN خارجی) | S |
| 4 | پاس کامل دسترس‌پذیری: ARIA Dialog، focus-trap، برچسب فرم، `prefers-reduced-motion` | M |
| 5 | ارتقای PHP floor به ۷.۴ + تکمیل `readme.txt` (FAQ/Screenshots/افشای سرویس خارجی) | S |
| 6 | زیرساخت کیفیت: PHPUnit + PHPCS + PHPStan + GitHub Actions | M |
| 7 | اسکریپت build (تولید zip تمیز بدون فایل‌های توسعه) | S |

**خروجی:** نسخهٔ قابل‌عرضه در بازار ایران. **← اینجا می‌توانید شروع به فروش کنید.**

---

### ⚡ نسخهٔ ۴.۵ — «حرفه‌ای» (تجربهٔ کاربری در سطح رقبای تجاری)

| # | کار | اندازه | چرا |
|---|---|---|---|
| 8 | **پاسخ استریم (SSE)** — نمایش توکن‌به‌توکن | M | مهم‌ترین ارتقای حس محصول؛ همچنین P-1 را حل می‌کند |
| 9 | **REST API** به‌جای admin-ajax | M | سرعت، کش، تست‌پذیری |
| 10 | **پایداری گفتگو بین صفحات** | S | الان با هر ناوبری گفتگو پاک می‌شود |
| 11 | **فرم‌ساز پویا** (Field Builder) | L | هر کسب‌وکار فرم خودش را بسازد — قابلیت فروش قوی |
| 12 | **بلوک گوتنبرگ** (کنار شورت‌کد و المنتور) | S | پوشش کامل بازار |
| 13 | **ایمپورت PDF/DOCX به پایگاه دانش** | M | الان فقط txt |

---

### 🌍 نسخهٔ ۵.۰ — «جهانی» (ورود به بازار بین‌المللی)

| # | کار | اندازه | چرا |
|---|---|---|---|
| 14 | **انگلیسی‌سازی کامل** — انگلیسی زبان پایه، فارسی به‌عنوان ترجمه | L | **مسدودکنندهٔ اصلی فروش جهانی** |
| 15 | **پشتیبانی دوجهته (LTR/RTL)** با خصوصیات منطقی CSS | M | بدون این، در بازار جهانی شکسته است |
| 16 | **بازنویسی معماری** (بخش ۵) | XL | بدهی فنی مانع رشد است |
| 17 | **بازنویسی فرانت‌اند** با build pipeline + Web Component | L | ۱۳۰۲ خط IIFE با رندر کامل DOM |
| 18 | **سیستم لایسنس و آپدیت** (Freemius یا Lemon Squeezy) | M | لازمهٔ فروش |
| 19 | **واگذاری به اپراتور زنده** | XL | تمایز رقابتی بزرگ |
| 20 | **RAG برداری واقعی** (با fallback آفلاین موجود) | L | دقت بالاتر، حفظ مزیت آفلاین |

---

## ۵. معماری هدف

**وضعیت فعلی → هدف:**

| اکنون | هدف |
|---|---|
| `SSC_Chatbot_Ajax` — ۱۳۸۶ خط، همه‌کاره | `interface AIProvider` + یک کلاس به‌ازای هر ارائه‌دهنده + `ProviderRegistry` قابل‌توسعه با فیلتر |
| فراخوانی مستقیم `$wpdb` در همه‌جا | لایهٔ Repository |
| `admin-ajax.php` | `REST /wp-json/ssc/v1/*` |
| بدون autoload، require دستی | PSR-4 + Composer autoload + namespace `SSC\` |
| IIFE ۱۳۰۲ خطی، رندر کامل DOM | Web Component / Preact + Vite + TypeScript، رندر افزایشی |
| بدون تست | PHPUnit (واحد) + Playwright (E2E) |
| بدون CI | GitHub Actions: phpcs → phpstan → tests → build zip |

**چرا `ProviderRegistry` قابل‌توسعه مهم است:** به توسعه‌دهندگان دیگر اجازه می‌دهد موتور جدید اضافه کنند بدون تغییر هستهٔ شما. این همان چیزی است که یک افزونه را به یک «پلتفرم» تبدیل می‌کند و اکوسیستم می‌سازد.

---

## ۶. مدل درآمدی — پیشنهاد مشخص

### تقسیم رایگان / Pro

| رایگان (WP.org — قیف بازاریابی) | Pro (فروش) |
|---|---|
| چت با همهٔ موتورهای AI | **فرم‌ساز پویا** |
| بانک پاسخ (سقف مثلاً ۵۰ ردیف) | بانک پاسخ نامحدود + ایمپورت انبوه |
| پایگاه دانش پایه (txt) | ایمپورت PDF/DOCX + RAG برداری |
| شورت‌کد + بلوک | ویجت المنتور + استایل پیشرفته |
| ظاهر پایه | **حذف برندینگ (White-label)** |
| ۱ فرم ساده | واگذاری به اپراتور زنده |
| آمار پایه | آنالیتیکس پیشرفته + خروجی گزارش |
| — | اتصال به WooCommerce |
| — | پشتیبانی اولویت‌دار |

> **نکتهٔ کلیدی:** «حذف برندینگ» کلاسیک‌ترین و مؤثرترین اهرم فروش در این دسته است.

### قیمت‌گذاری (بازهٔ متعارف صنعت — نیازمند اعتبارسنجی بازار)

- **بین‌المللی:** تک‌سایت ~$۴۹–۷۹/سال · ۵ سایت ~$۹۹–۱۴۹ · نامحدود/آژانس ~$۱۹۹–۲۹۹ · مادام‌العمر ≈ ۳–۴ برابر سالانه
- **ایران:** قیمت‌گذاری را با نگاه به ژاکت/راست‌چین انجام دهید؛ مدل «یک‌بار خرید + ۱ سال آپدیت» در این بازار پذیرفته‌تر است.

---

## ۷. زیرساخت انتشار (بدون این‌ها فروش پایدار نیست)

| مورد | وضعیت | اقدام |
|---|---|---|
| **مستندات کاربر** | ندارد | راهنمای نصب، تنظیم هر موتور AI، شورت‌کد، هوک‌ها، FAQ |
| **سایت دمو زنده** | ندارد | حیاتی برای فروش — خریدار باید قبل از خرید لمس کند |
| **ویدیوی معرفی ۲ دقیقه‌ای** | ندارد | بیشترین اثر روی نرخ تبدیل |
| **کانال پشتیبانی** | ندارد | تیکت/ایمیل + SLA مشخص |
| **Changelog و نسخه‌بندی** | ✅ دارد | حفظ شود |
| **سیستم لایسنس/آپدیت خودکار** | ندارد | Freemius (ساده‌ترین) یا Lemon Squeezy |
| **صفحهٔ فروش (Landing)** | ندارد | تمرکز روی سه تمایز بخش ۲ |

---

## ۸. سه اقدام بعدی (اگر فقط یک هفته وقت دارید)

۱. **رفع ۵ مورد امنیتی + ۵ مورد داده** (بخش ۳) — این‌ها ریسک واقعی برای کاربر شما هستند.
۲. **میزبانی محلی فونت‌ها** — یک تغییر کوچک با اثر بزرگ (پایداری + حریم خصوصی).
۳. **راه‌اندازی CI با PHPCS** — از این به بعد هر تغییر خودکار بررسی می‌شود؛ کیفیت دیگر افت نمی‌کند.

---

## ۹. نظر صادقانه و ریسک‌ها

**آنچه واقعاً خوب است:** هستهٔ فنی — RAG آفلاین، معماری چندموتوره، و مدیریت درخواست‌ها — ارزش تجاری واقعی دارد و بازسازی آن از صفر ماه‌ها طول می‌کشد. سرمایه‌گذاری روی این پایه منطقی است.

**ریسک‌های اصلی که باید بپذیرید:**

1. **بازار جهانی بسیار شلوغ است.** بدون تمایز روشن (بخش ۲) و بدون بودجهٔ بازاریابی، صرفاً «خوب بودن» کافی نیست. بازار ایران ریسک بسیار کمتری دارد.
2. **پشتیبانی هزینهٔ پنهان است.** فروش یعنی تعهد پاسخ‌گویی. اگر ظرفیت آن را ندارید، مدل «یک‌بار فروش بدون پشتیبانی» صادقانه‌تر است.
3. **هزینهٔ API با مشتری است، نه شما** — این را در صفحهٔ فروش شفاف کنید تا انتظار غلط ایجاد نشود.
4. **بدهی فنی مانع سرعت است.** هر قابلیت جدیدی که روی معماری فعلی اضافه کنید، هزینهٔ بازنویسی آینده را بالا می‌برد. توصیه: قبل از نسخهٔ ۵ سراغ قابلیت‌های بزرگ نروید.

**اگر یک چیز را انتخاب کنم:** **تست و CI (کار #۶).** نه به‌خاطر خودش، بلکه چون بدون آن، هر ارتقای بزرگ بعدی ریسک شکستن چیزهای کارکننده را دارد — و این تنها چیزی است که «افزونهٔ شخصی» را از «محصول قابل‌اتکا» جدا می‌کند.
